import * as stylex from "@stylexjs/stylex";

import { backgroundColors } from "../../tokens/semantics.stylex";

export const codeEditorVars = stylex.defineVars({
  backgroundColor: backgroundColors["bg-card"],
});
