import * as stylex from '@stylexjs/stylex'

import { layerIndexes } from '../../tokens/layers.stylex'
import {
  accentElementColors,
  borderColors,
  focusColors,
  ghostElementColors,
  spatial,
  surfaceColors,
  textColors,
} from '../../tokens/semantics.stylex'
import {
  borderRadii,
  fontFamilies,
  fontSizes,
  fontWeights,
  lineHeights,
  shadows,
  spacing,
} from '../../tokens/value.stylex'

export const calendarStyles = stylex.create({
  trigger: {
    borderColor: borderColors.default,
    borderRadius: borderRadii.xs,
    borderStyle: 'solid',
    borderWidth: 1,
    overflow: 'hidden',
    paddingInline: spacing.xs,
    alignItems: 'center',
    appearance: 'none',
    backgroundColor: {
      default: surfaceColors.default,
      ':hover': ghostElementColors.hover,
      ':active': ghostElementColors.pressed,
    },
    color: textColors.default,
    cursor: 'pointer',
    display: 'inline-flex',
    fontFamily: fontFamilies.mono,
    fontSize: fontSizes[2],
    lineHeight: lineHeights.ui,
    textAlign: 'center',
    textOverflow: 'ellipsis',
    whiteSpace: 'nowrap',
    height: spatial['control-height-l'],
    minWidth: 0,
    width: '100%',
  },
  triggerOpen: { backgroundColor: ghostElementColors.selected },
  triggerClosed: {},
  triggerDisabled: {
    backgroundColor: surfaceColors.default,
    color: textColors.disabled,
    cursor: 'not-allowed',
  },
  positioner: { outline: 'none', zIndex: layerIndexes.popup },
  positionerOpen: {},
  positionerClosed: {},
  positionerAnchorHidden: {},
  positionerInstant: {},
  positionerSideTop: {},
  positionerSideBottom: {},
  positionerSideLeft: {},
  positionerSideRight: {},
  positionerSideInlineStart: {},
  positionerSideInlineEnd: {},
  positionerAlignStart: {},
  positionerAlignCenter: {},
  positionerAlignEnd: {},
  popup: {
    padding: spacing.m,
    borderColor: borderColors.default,
    borderRadius: borderRadii.xs,
    borderStyle: 'solid',
    borderWidth: 1,
    outline: 'none',
    backgroundColor: surfaceColors.raised,
    boxShadow: shadows.medium,
    color: textColors.default,
    display: 'flex',
    flexDirection: 'column',
    transformOrigin: 'var(--transform-origin)',
    maxHeight: 'var(--available-height)',
    maxWidth: 'var(--available-width)',
    width: spatial['multi-select-width-m'],
  },
  popupOpen: {},
  popupClosed: {},
  popupStarting: { opacity: 0, transform: 'scale(0.98)' },
  popupEnding: { opacity: 0, transform: 'scale(0.98)' },
  popupInstant: {},
  popupSideTop: {},
  popupSideBottom: {},
  popupSideLeft: {},
  popupSideRight: {},
  popupSideInlineStart: {},
  popupSideInlineEnd: {},
  popupAlignStart: {},
  popupAlignCenter: {},
  popupAlignEnd: {},
  calendarRoot: { fontFamily: fontFamilies.sans, width: '100%' },
  months: { display: 'flex', width: '100%' },
  month: {
    gap: spacing.s,
    display: 'flex',
    flexDirection: 'column',
    position: 'relative',
    width: '100%',
  },
  monthCaption: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'center',
    minHeight: spatial['interaction-target-min'],
  },
  dropdowns: { gap: spacing.xs, alignItems: 'center', display: 'flex' },
  dropdownRoot: {
    borderRadius: borderRadii.xs,
    outlineColor: focusColors.ring,
    outlineOffset: -1,
    outlineStyle: 'solid',
    outlineWidth: { default: 0, ':focus-within': spatial['focus-ring-width'] },
    position: 'relative',
  },
  dropdown: {
    inset: 0,
    cursor: 'pointer',
    opacity: 0,
    position: 'absolute',
    height: '100%',
    width: '100%',
  },
  captionLabel: {
    gap: spacing.xxs,
    paddingInline: spacing.xs,
    alignItems: 'center',
    display: 'inline-flex',
    fontSize: fontSizes[2],
    fontWeight: fontWeights.medium,
    lineHeight: lineHeights.ui,
    minHeight: spatial['interaction-target-min'],
  },
  nav: {
    insetInline: 0,
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'space-between',
    pointerEvents: 'none',
    position: 'absolute',
    height: spatial['interaction-target-min'],
    top: 0,
  },
  navButton: {
    padding: 0,
    borderRadius: borderRadii.xs,
    borderWidth: 0,
    alignItems: 'center',
    appearance: 'none',
    backgroundColor: {
      default: 'transparent',
      ':hover': ghostElementColors.hover,
      ':active': ghostElementColors.pressed,
    },
    color: { default: textColors.muted, ':disabled': textColors.disabled },
    cursor: { default: 'pointer', ':disabled': 'not-allowed' },
    display: 'inline-flex',
    justifyContent: 'center',
    outlineColor: focusColors.ring,
    outlineOffset: -1,
    outlineStyle: 'solid',
    outlineWidth: { default: 0, ':focus-visible': spatial['focus-ring-width'] },
    pointerEvents: 'auto',
    height: spatial['interaction-target-min'],
    width: spatial['interaction-target-min'],
  },
  navButtonPrevious: { insetInlineStart: 0, position: 'absolute', top: 0 },
  navButtonNext: { insetInlineEnd: 0, position: 'absolute', top: 0 },
  chevron: { display: 'block', height: spatial['icon-size-m'], width: spatial['icon-size-m'] },
  monthGrid: { borderCollapse: 'collapse', tableLayout: 'fixed', width: '100%' },
  weekday: {
    color: textColors.muted,
    fontSize: fontSizes[1],
    fontWeight: fontWeights.regular,
    lineHeight: lineHeights.compact,
    textAlign: 'center',
    height: spatial['interaction-target-min'],
  },
  day: { padding: spacing.xxs, textAlign: 'center' },
  dayButton: {
    padding: 0,
    borderRadius: borderRadii.xs,
    borderWidth: 0,
    alignItems: 'center',
    appearance: 'none',
    backgroundColor: {
      default: 'transparent',
      ':hover': ghostElementColors.hover,
      ':active': ghostElementColors.pressed,
    },
    color: textColors.default,
    cursor: 'pointer',
    display: 'inline-flex',
    fontFamily: fontFamilies.sans,
    fontSize: fontSizes[2],
    fontVariantNumeric: 'tabular-nums',
    justifyContent: 'center',
    lineHeight: lineHeights.ui,
    outlineColor: focusColors.ring,
    outlineOffset: -1,
    outlineStyle: 'solid',
    outlineWidth: { default: 0, ':focus-visible': spatial['focus-ring-width'] },
    height: spatial['interaction-target-min'],
    width: spatial['interaction-target-min'],
  },
  daySelected: {
    backgroundColor: {
      default: accentElementColors.default,
      ':hover': accentElementColors.hover,
      ':active': accentElementColors.pressed,
    },
    color: textColors.onAccent,
  },
  dayOutside: { color: textColors.disabled },
  dayDisabled: { color: textColors.disabled, cursor: 'not-allowed' },
  dayHidden: { visibility: 'hidden' },
  controls: {
    gap: spacing.m,
    display: 'flex',
    flexDirection: 'column',
    borderTopColor: borderColors.subtle,
    borderTopStyle: 'solid',
    borderTopWidth: 1,
    paddingTop: spacing.l,
  },
  controlLabel: {
    color: textColors.muted,
    fontFamily: fontFamilies.sans,
    fontSize: fontSizes[1],
    lineHeight: lineHeights.compact,
  },
  field: {
    gap: spacing.xs,
    display: 'flex',
    flexDirection: 'column',
    flexGrow: 1,
    flexShrink: 1,
    minWidth: 0,
  },
  input: {
    textAlign: 'center',
  },
  actions: { gap: spacing.m, display: 'flex', flexDirection: 'column' },
})
