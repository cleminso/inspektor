import * as stylex from "@stylexjs/stylex";

import { textColors } from "../../tokens/semantics.stylex";

export const comboboxStyleVars = stylex.defineVars({
  triggerTextColor: textColors["text-default"],
});
