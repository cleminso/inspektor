import * as stylex from "@stylexjs/stylex";

import { layerIndexes } from "../../tokens/layers.stylex";
import { backgroundColors, borderColors, textColors } from "../../tokens/semantics.stylex";
import {
  borderRadii,
  fontFamilies,
  fontSizes,
  fontWeights,
  lineHeights,
  spacing,
} from "../../tokens/value.stylex";

export const selectStyles = stylex.create({
  label: {
    color: textColors["text-default"],
    cursor: "default",
    fontFamily: fontFamilies.sans,
    fontSize: fontSizes[1],
    fontWeight: fontWeights.medium,
    lineHeight: lineHeights.tight,
  },
  labelDisabled: { color: textColors["text-disabled"] },
  trigger: {
    borderColor: {
      default: borderColors.border,
      ":focus-visible": borderColors["border-focused"],
    },
    borderRadius: borderRadii.xs,
    borderStyle: "solid",
    borderWidth: 1,
    gap: spacing.l,
    alignItems: "center",
    appearance: "none",
    backgroundColor: {
      default: backgroundColors["bg-card"],
      ":hover": backgroundColors["bg-hover"],
      ":active": backgroundColors["bg-pressed"],
    },
    color: textColors["text-default"],
    cursor: "pointer",
    display: "inline-flex",
    fontFamily: fontFamilies.sans,
    fontSize: fontSizes[2],
    justifyContent: "space-between",
    lineHeight: lineHeights.none,
    outlineColor: borderColors["border-focused"],
    outlineOffset: -1,
    outlineStyle: "solid",
    outlineWidth: 0,
    userSelect: "none",
    whiteSpace: "nowrap",
    minWidth: 160,
  },
  sizeS: { height: 24, paddingInline: spacing.s },
  sizeM: { height: 28, paddingInline: spacing.m },
  sizeL: { height: 32, paddingInline: spacing.l },
  triggerWidthContent: { width: "max-content" },
  triggerWidthFull: { width: "100%" },
  triggerOpen: { backgroundColor: backgroundColors["bg-selected"] },
  triggerInvalid: {
    borderColor: {
      default: borderColors["border-danger"],
      ":focus-visible": borderColors["border-danger"],
    },
    outlineColor: borderColors["border-danger-subtle"],
    outlineOffset: 0,
    outlineStyle: "solid",
    outlineWidth: 2,
  },
  disabled: {
    borderColor: borderColors.border,
    backgroundColor: {
      default: backgroundColors["bg-card"],
      ":hover": backgroundColors["bg-card"],
      ":active": backgroundColors["bg-card"],
    },
    color: textColors["text-disabled"],
    cursor: "not-allowed",
    outlineWidth: 0,
  },
  valuePlaceholder: { color: textColors["text-subtle"] },
  adornment: { alignItems: "center", display: "inline-flex", flexShrink: 0 },
  iconContainer: { alignItems: "center", display: "inline-flex", flexShrink: 0 },
  icon: { display: "block", height: 14, width: 14 },
  positioner: { outline: "none", userSelect: "none", zIndex: layerIndexes.popup },
  popup: {
    borderColor: borderColors.border,
    borderRadius: borderRadii.xs,
    borderStyle: "solid",
    borderWidth: 1,
    outline: "none",
    backgroundColor: backgroundColors["bg-popover"],
    color: textColors["text-default"],
    transformOrigin: "var(--transform-origin)",
    minWidth: "var(--anchor-width)",
  },
  popupTransition: { opacity: 0, transform: "scale(0.98)" },
  list: {
    padding: spacing.xxs,
    gap: spacing.xxs,
    outline: "none",
    overscrollBehavior: "contain",
    display: "flex",
    flexDirection: "column",
    maxHeight: "var(--available-height)",
    overflowY: "auto",
  },
  item: {
    borderRadius: borderRadii.xs,
    gap: spacing.m,
    outline: "none",
    alignItems: "center",
    backgroundColor: { default: "transparent", ":active": backgroundColors["bg-pressed"] },
    boxSizing: "border-box",
    cursor: "default",
    display: "grid",
    fontFamily: fontFamilies.sans,
    fontSize: fontSizes[2],
    gridTemplateColumns: "minmax(0, 1fr) 16px",
    lineHeight: lineHeights.tight,
    userSelect: "none",
    width: "100%",
  },
  itemSizeS: { minHeight: 24, paddingInline: spacing.s },
  itemSizeM: { minHeight: 28, paddingInline: spacing.s },
  itemSizeL: { minHeight: 32, paddingInline: spacing.m },
  itemHighlighted: {
    backgroundColor: backgroundColors["bg-hover"],
  },
  itemSelected: { backgroundColor: backgroundColors["bg-selected"] },
  itemDisabled: {
    backgroundColor: { default: "transparent", ":active": "transparent" },
    color: textColors["text-disabled"],
  },
  itemText: { gridColumnStart: "1", minWidth: 0 },
  indicator: {
    alignItems: "center",
    display: "inline-flex",
    gridColumnStart: "2",
    justifyContent: "center",
    justifySelf: "end",
  },
  group: { paddingBlock: spacing.xxs },
  groupLabel: {
    paddingBlock: spacing.xxs,
    paddingInline: spacing.m,
    color: textColors["text-subtle"],
    fontFamily: fontFamilies.sans,
    fontSize: fontSizes[1],
    fontWeight: fontWeights.medium,
    lineHeight: lineHeights.tight,
  },
  separator: {
    marginBlock: spacing.xs,
    backgroundColor: borderColors["border-secondary"],
    height: 1,
  },
});
