import { createText } from '../../primitives/createText'

export const Text = createText()
export type {
  TextAlign,
  TextColor,
  TextFormatter,
  TextProps,
  TextStyleProps,
  TextTag,
  TextVariant,
  TextWrap,
} from '../../primitives/createText'
