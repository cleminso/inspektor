import * as stylex from '@stylexjs/stylex'

export const layerIndexes = stylex.defineConsts({
  content: 1,
  navigation: 10,
  floating: 50,
  popup: 100,
  tooltip: 200,
  overlay: 300,
  modal: 400,
  toast: 500,
  drag: 600,
})
