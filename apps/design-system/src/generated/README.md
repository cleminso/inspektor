# Generated Props

`props.json` is reserved for generated prop metadata from `packages/design-system`.

Docs pages should stay curated manually. Generated metadata can fill in TypeScript types and source references once real components are exported.
