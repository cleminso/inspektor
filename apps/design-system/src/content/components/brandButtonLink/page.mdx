import { ComponentDemo } from '../../../components/docs/componentDemo'
import DefaultDemo from './demos/defaultDemo'
import defaultSource from './demos/defaultDemo.tsx?raw'

## Primary brand navigation

Use the brand button link for the primary navigation action on brand surfaces. It renders a native anchor and accepts the destination through `href`.

<ComponentDemo source={defaultSource}>
  <DefaultDemo />
</ComponentDemo>
