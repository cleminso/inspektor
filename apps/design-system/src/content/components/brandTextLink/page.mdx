import { ComponentDemo } from '../../../components/docs/componentDemo'
import DefaultDemo from './demos/defaultDemo'
import defaultSource from './demos/defaultDemo.tsx?raw'

## Neutral brand links

Use the brand text link for secondary navigation and external links on brand surfaces. It keeps the brand typography and neutral color treatment while rendering a native anchor.

<ComponentDemo source={defaultSource}>
  <DefaultDemo />
</ComponentDemo>
