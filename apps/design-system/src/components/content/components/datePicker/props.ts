export const datePickerRootPropNames = [
  "value",
  "onApply",
  "open",
  "defaultOpen",
  "onOpenChange",
  "minValue",
  "maxValue",
  "disabled",
] as const;
export const datePickerTriggerPropNames = ["label", "render"] as const;
export const datePickerContentPropNames = ["align", "autoFocus", "keepMounted"] as const;
export const datePickerPanelPropNames = ["autoFocus"] as const;
