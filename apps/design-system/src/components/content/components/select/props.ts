export const selectRootPropNames = [
  "items",
  "value",
  "defaultValue",
  "onValueChange",
  "disabled",
] as const;
export const selectTriggerPropNames = ["placeholder", "size", "width"] as const;
export const selectItemPropNames = ["value", "disabled", "label"] as const;
