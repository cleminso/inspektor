export const menuRootPropNames = ["defaultOpen", "open", "onOpenChange", "disabled"] as const;
export const menuTriggerPropNames = ["disabled", "render"] as const;
export const menuPositionerPropNames = ["sideOffset", "align"] as const;
export const menuPopupPropNames = ["width"] as const;
export const menuGroupPropNames = ["render"] as const;
export const menuGroupLabelPropNames = ["render"] as const;
export const menuShortcutPropNames = ["render"] as const;
export const menuItemPropNames = ["variant", "disabled", "closeOnClick"] as const;
export const menuCheckboxItemPropNames = [
  "checked",
  "defaultChecked",
  "onCheckedChange",
  "disabled",
] as const;
