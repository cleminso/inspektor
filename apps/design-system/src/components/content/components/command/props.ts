export const commandRootPropNames = ["items", "autoHighlight", "inputValue", "defaultInputValue", "onInputValueChange"] as const;
export const commandDialogPropNames = ["open", "defaultOpen", "onOpenChange", "keepMounted", "initialFocus", "finalFocus"] as const;
export const commandInputPropNames = ["divider"] as const;
export const commandItemPropNames = ["value", "disabled"] as const;
export const commandItemTextPropNames = ["label", "description", "layout"] as const;
