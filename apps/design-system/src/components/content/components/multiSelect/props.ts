export const multiSelectRootPropNames = [
  "items",
  "value",
  "defaultValue",
  "onValueChange",
  "defaultOpen",
  "open",
  "onOpenChange",
  "disabled",
] as const;

export const multiSelectTriggerPropNames = ["label", "render", "disabled"] as const;

export const multiSelectContentPropNames = [
  "label",
  "width",
  "maxHeight",
  "keepMounted",
  "align",
] as const;
