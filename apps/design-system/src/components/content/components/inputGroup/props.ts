export const inputGroupRootPropNames = ["size", "fullWidth", "invalid", "disabled"] as const;
export const inputGroupPrefixPropNames = ["children"] as const;
export const inputGroupSuffixPropNames = ["children"] as const;
export const inputGroupActionPropNames = [
  "label",
  "controls",
  "pressed",
  "disabled",
  "onClick",
  "children",
] as const;
export const inputGroupCheckboxPropNames = [
  "label",
  "checked",
  "defaultChecked",
  "onCheckedChange",
  "disabled",
  "readOnly",
  "children",
] as const;
