export const tooltipProviderPropNames = ["delay", "closeDelay", "timeout"] as const;
export const tooltipRootPropNames = [
  "defaultOpen",
  "open",
  "onOpenChange",
  "onOpenChangeComplete",
  "disableHoverablePopup",
  "trackCursorAxis",
  "actionsRef",
  "disabled",
] as const;
export const tooltipTriggerPropNames = [
  "delay",
  "closeOnClick",
  "closeDelay",
  "disabled",
  "render",
] as const;
export const tooltipContentPropNames = ["side", "align", "sideOffset"] as const;
