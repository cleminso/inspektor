export const rootPropNames = ["disabled", "invalid", "invalidDescription"] as const;
export const triggerPropNames = ["aria-label", "disabled"] as const;
export const removePropNames = ["aria-label", "disabled"] as const;
