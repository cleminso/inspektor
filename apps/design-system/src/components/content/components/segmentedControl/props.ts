export const segmentedControlRootPropNames = [
  "value",
  "defaultValue",
  "onValueChange",
] as const;

export const segmentedControlListPropNames = [
  "aria-label",
  "aria-labelledby",
  "width",
  "loopFocus",
] as const;

export const segmentedControlItemPropNames = ["value", "disabled"] as const;

export const segmentedControlPanelPropNames = ["value", "keepMounted"] as const;
