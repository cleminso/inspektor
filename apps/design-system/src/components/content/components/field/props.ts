export const fieldRootPropNames = [
  'disabled',
  'name',
  'invalid',
  'dirty',
  'touched',
  'validate',
  'validationMode',
  'validationDebounceTime',
  'actionsRef',
  'render',
] as const

export const fieldLabelPropNames = ['nativeLabel', 'render'] as const

export const fieldDescriptionPropNames = ['render'] as const

export const fieldErrorPropNames = ['match', 'render'] as const
